# anz-schedule-brain — environment variables
#
# Copy this file to .env and fill in your values.
# For local stdio development, you can leave MCP_API_KEY blank (open access).
# For hosted SSE deployment, set a strong secret key.

# API key required on every SSE request via the x-api-key header.
# Leave blank to run in open-access mode (local dev only).
MCP_API_KEY=

# Transport mode: "sse" for remote/Railway deployment, "stdio" for local Claude Desktop.
# Defaults to "stdio" if not set.
MCP_TRANSPORT=stdio

# Port for the SSE HTTP server (only used when MCP_TRANSPORT=sse).
# Defaults to 3000 if not set.
PORT=3000
