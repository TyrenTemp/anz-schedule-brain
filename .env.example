# ──────────────────────────────────────────────────────────────────────────────
# anz-schedule-brain — environment variables
# Copy this file to .env and fill in values. Never commit .env.
# ──────────────────────────────────────────────────────────────────────────────

# Transport mode: "sse" for Railway/remote, "stdio" for local Claude Desktop use.
MCP_TRANSPORT=stdio

# Port for SSE transport (Railway sets this automatically).
PORT=3000

# API key for header-based authentication (x-api-key).
# Leave blank to allow unauthenticated access (local dev only).
# Set a strong random value in production.
MCP_API_KEY=

# Example: MCP_API_KEY=sk-anz-prod-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
